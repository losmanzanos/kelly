{
  "name": "coolbirdcounseling",
  "version": "1.0.0",
  "private": true,
  "description": "Cool Bird Counseling LLC \u2014 static site",
  "scripts": {
    "build": "node build.js",
    "build:cms": "tinacms build && node build.js",
    "dev": "npx wrangler pages dev . --compatibility-date=2026-01-01",
    "tina": "tinacms dev -c \"node build.js && npx --yes serve . -l 3000\"",
    "tina:lock": "tinacms build"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@tinacms/cli": "^2.6.1",
    "tinacms": "^3.12.1"
  }
}
