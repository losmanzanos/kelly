node_modules/
.wrangler/
.env
.env.*
.DS_Store
tina/__generated__/

# Generated by `tinacms build` on every deploy — not source.
admin/

# tina-lock.json is deliberately NOT ignored: TinaCloud reads it from the repo
# to index content, so it has to be committed.
